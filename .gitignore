*.class
.gradle/
build/
.idea/
.vscode/
out/
local.properties
*.iml

# Local secrets — never commit real credentials.
run-local.secrets.ps1
.env
.env.local

# Local verification / shared image storage (never commit artifacts).
.tmp/
